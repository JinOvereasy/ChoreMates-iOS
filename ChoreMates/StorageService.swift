//
//  StorageService.swift
//  ChoreMates
//
//  Created by James Kang on 7/25/17.
//  Copyright © 2017 James Kang. All rights reserved.
//

import Foundation
import UIKit
import FirebaseStorage

struct StorageService {
 
    /*
    static func uploadChore(_ chore: Chore, at reference: StorageReference, completion: @escaping (URL?) -> Void) {
    
        // 1
        guard let choreData =
        return completion(nil)
    }
    
    // 2
    reference.putData(choreData, metadata: nil, completion: { (metadata, error) in
        // 3
    if let error = error {
        assertionFailure(error.localizedDescription)
        return completion(nil)
    }
    
    // 4
    completion(metadata?.downloadURL())
    })
    */
    
}
